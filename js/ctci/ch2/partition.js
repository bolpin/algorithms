// partition list around a value
// nodes to the left are < val
// nodes to the right are >= val
function partition(val) {
	// traverse list with two iterators

	// keep an additional list or data structurek
	// for any list < x, remove from list and
	// insert at front of list
	
	// when we find x, continue 
	// with the leading iterator 

}