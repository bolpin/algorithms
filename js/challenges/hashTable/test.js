const { HashTable } = require('./index');

test('HashTable is defined', () => {
  expect(HashTable).toBeDefined();
});

// test('Calling fizzbuzz with `5` prints out 5 statements', () => {
//   expect(console.log.mock.calls.length).toEqual(5);
// });
