const combinations = require('./index');

test('Combinations function exists', () => {
  expect(combinations).toBeDefined();
});

// test('Finds combinations of a string', () => {
//   expect(combinations('123')).toEqual(
// 	  );
// });
